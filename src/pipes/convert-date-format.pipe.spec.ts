import { ConvertDateFormatPipe } from './convert-date-format.pipe';

describe('ConvertDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
