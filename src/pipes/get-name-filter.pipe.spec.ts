import { GetNameFilterPipe } from './get-name-filter.pipe';

describe('GetNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
